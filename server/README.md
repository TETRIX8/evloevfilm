# Production runtime

`index.mjs` is the server-side runtime currently deployed under `/opt/tetrixfilm/server/index.mjs` on the production host.

Secrets and user data are not stored in this repository.
